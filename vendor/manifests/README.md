# Vendor manifests

Vendor manifests record immutable upstream inputs, integrity, license, and provenance. Downloaded archives belong in ignored `vendor/cache/` and are never authoritative without a matching manifest and verification result.

# Distribution input manifests

These are the input manifests for the 0.2.1 release builder (Phase B, milestone R6, plus the 0.2.1 sidecar-lifecycle fix). They are not installers and do not claim that Ayran is runnable. Layer treats Prime as an externally owned peer; Complete owns an isolated unmodified pinned Prime prefix.

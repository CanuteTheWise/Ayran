# M0 tests

M0 tests cover contracts, generation, compatibility/distribution inputs, canonical JSON serialization, and safe staging only. Graph, router, adapter, evidence-gate, and audit behavior tests begin in later milestones.

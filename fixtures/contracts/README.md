# Contract fixture corpus

Each contract entrypoint has `valid/minimal.json`, `valid/full.json`, and security-relevant files under `invalid/`. `manifest.json` is the authoritative expected-outcome index consumed by JSON Schema, Pydantic, and Ajv parity tests.
